# URL publica do back-end (Express + Socket.IO). Em desenvolvimento local pode
# ser omitida (o app usa http://localhost:5000 por padrao). Em producao,
# configure com a URL real onde o servidor esta publicado.
VITE_API_URL=http://localhost:5000
